{
  "name": "letstouchbase",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace apps/mobile",
    "android": "npm run android --workspace apps/mobile"
  }
}
